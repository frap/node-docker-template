{
  "name": "docker-overide-demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ramda": "^0.26.1"
  },
  "scripts": {
    "start": "yarn start",
    "test": "jest --watchAll",
    "coverage": "jest test -- --coverage --coverageReporters=text",
    "lint": "eslint"

  },
  "devDependencies": {
    "eslint": "^6.2.1",
    "eslint-plugin-jest": "^22.15.2",
    "jest": "^24.9.0"
  }
}
