{
  "name": "docker-overide-demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ramda": "^0.26.1"
  },
  "scripts": {
    "start": "yarn start",
    "test": "jest --watchAll",
    "coverage": "jest --coverage --coverageReporters=text",
    "lint": "eslint . && prettier-eslint --list-different **/*.js",
    "format": "prettier-eslint --write **/*.js"
  },
  "devDependencies": {
    "eslint": "^6.2.1",
    "eslint-plugin-jest": "^22.15.2",
    "jest": "^24.9.0",
    "prettier-eslint-cli": "^5.0.0"
  }
}
